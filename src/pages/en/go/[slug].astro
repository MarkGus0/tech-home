---
import RedirectLayout from "@layouts/RedirectLayout.astro";
import { goLinks } from "@data/forms";

export function getStaticPaths() {
  return Object.keys(goLinks).map((slug) => ({ params: { slug } }));
}
---
<RedirectLayout locale="en" />
